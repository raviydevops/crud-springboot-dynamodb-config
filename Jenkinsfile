pipeline {
    agent any
    stages {
        stage ('git-clone') {
            steps {
                echo "Deploying to local and aws"
                error 'failing on purpose'
            }
        }
    }
}
